# 统一换行符：仓库内默认 LF，避免 Windows 上反复出现 CRLF 改动
* text=auto eol=lf

# Windows 批处理必须保持 CRLF：cmd.exe 对 LF 结尾的 .bat/.cmd 支持不可靠
# （多行括号块、goto/label 等会出问题），因此这两类文件单独指定
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制资源不做换行符转换（地图贴图、Unity 网格数据、音视频）
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary
*.bytes binary
*.mp3 binary
*.ogg binary
*.wav binary
*.mp4 binary
*.webm binary
*.woff2 binary
